# 第一版验证记录

验证日期：2026 年 9 月 10 日。交付范围为公司内部评审及讲师投屏。

## 已完成

- TypeScript 类型检查、ESLint、Prettier 检查通过。
- Vitest 的 5 项测试通过，覆盖课程数据约束、学习记录损坏及类型校验、路由范围和计时截止时间。
- Vite 生产构建通过。发布目录共 4 个文件、207862 字节；未包含原始聊天、逐字稿及部署凭据。
- 浏览器检查 36 个课程页面状态，均有唯一主标题，移动端无页面横向溢出；另检查桌面及平板布局。
- 在浏览器验证课程切换、讲师提示、计时开始与暂停、重置、无效时长、自检及笔记刷新恢复。
- 实际下载并读取实操记录 Markdown 和 Q3 模拟讨论稿。
- 生产预览已打开并操作；浏览器错误日志返回空列表。

## 验证边界

- 这些检查确认网页自身功能，不代表学员已完成实际 Agent、Skill、自学或 48 小时循环任务。实际执行与结果仍须在培训和课后验收。
- 离线评审 HTML 已生成，完成内嵌脚本语法检查、外部脚本及样式引用检查、Word 素材嵌入检查。浏览器工具的 URL 策略拒绝打开本地文件，因此尚未实测双击离线文件运行。
- 公司服务器已上传文件，工号由用户确认为 FD-222581。公司 OA 登录校验已生效：匿名访问返回 302 登录跳转，浏览器完成公司登录后成功进入课程。
- 已在静态源站读取 4 个发布文件，逐一比对大小和 SHA-256，全部与本地发布产物一致。
- 项目登记已完成：取得企业级 user_id 后，使用指定发布器的项目登记函数写入，公司接口返回 HTTP 201。回读确认唯一记录 ID 1842，工号 FD-222581，名称 suofou-ai-workshop，类型静态网页，访问地址全部匹配。
- 最终公司地址为 `https://app.fandow.top/fd-222581/suofou-ai-workshop/`。复查匿名访问返回 302 公司登录跳转；已登录浏览器能打开首页、课程及资料库。已点击原 Q3 Word 下载，浏览器错误日志为空。

## 本次服务器修复

现有通用静态路由设置为 `auth_request off`。已仅为 `/fd-222581/suofou-ai-workshop/` 添加项目专属 OA 鉴权配置；没有修改通用路由及其他员工项目。Nginx 配置检查和重载均通过。项目配置路径为 `/etc/nginx/lightdeploy-locations/per-app/fd-222581-suofou-ai-workshop.conf`。

## 发布完成

内部评审版发布、OA 鉴权、源站指纹和项目记录均已验证。课堂效果与 48 小时任务成果仍待实际培训后验收。
